import { WeekDayLabelPipe } from './week-day-label.pipe';

describe('WeekDayLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new WeekDayLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
